export enum RtcClientEvent {
    connected = "connected", //the socket is connected to the server

    disconnected = "disconnected", //the socket is dis connected to the server

    invite = "invite",//somebody call you
}